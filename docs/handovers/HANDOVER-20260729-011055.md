# DevPilot 交接文档

> 生成原因：batch-completed
> 生成时间：2026-07-29T01:10:55+08:00
> 总体完成度：90%

## 当前状态

- 当前批次：`B8`
- 下一步：进入 B8：稳定化、恢复、安全、部署与发布验收
- 工作区：`C:\Users\Admin\Desktop\workspace\DevPilot`
- Git 状态：changes present

## 批次进度

| 批次 | 状态 | 完成度 | 说明 |
|---|---|---:|---|
| B0 阶段 0：基础骨架与交接机制 | 已完成（本批次） | 100% | Monorepo, FastAPI, CLI, 契约, 交接 Agent |
| B1 阶段 1：模型与 LangGraph 内核 | 已完成（本批次） | 100% | LangGraph, 模型网关, 运行生命周期 |
| B2 阶段 2：工具、策略与人工审批 | 已完成（本批次） | 100% | 工具, 策略, Shell/Git, 审批, 审计 |
| B3 阶段 3：数据、记忆与项目上下文 | 已完成（本批次） | 100% | 数据库, 会话记忆, 项目规则, 长期记忆 |
| B4 阶段 4：研发工作流 | 已完成（本批次） | 100% | 仓库分析, 证据链, Agent 执行树, 测试, Worktree, PR |
| B5 阶段 5：CLI 与 Web 工作台 | 已完成（本批次） | 100% | CLI, React Web, 会话事件, 研发视图 |
| B6 阶段 6：Windows 本地 Web 与跨平台接口 | 已完成（本批次） | 100% | FastAPI, Vite, Windows 本地 Web, 平台端口, 浏览器启动 |
| B7 阶段 7：团队与共享 | 已完成（本批次） | 100% | 团队共享, RBAC, 远程 Host |
| B8 阶段 8：稳定化与发布 | 未开始 | 0% | 稳定化, 发布 |

## 已完成

- 建立基础分层、FastAPI、进度事实源和交接 Agent
- 实现状态图、模型适配器、事件序列和 checkpoint
- 实现工具注册、风险策略、文件/Shell/Git、审批和审计
- 实现 SQLite/Alembic、会话、项目、规则和长期记忆 API
- 实现仓库扫描、证据链、Agent 调度、测试和 PR 文档
- 实现 CLI、React/Vite 工作台、WebSocket 事件流和生产构建
- 修订计划书为 Windows-first 并保存原计划快照
- FastAPI 托管 Vite dist、静态资源和 SPA 回退
- CLI serve 支持浏览器启动、端口预检和明确启动错误
- 项目登记执行路径规范化、存在性与目录校验
- 提供 runtime logs API 并记录服务、项目和会话事件
- Web 工作台支持登记项目、创建会话和 WebSocket 首次对话
- 新增平台端口接口：PathResolver、ProcessRunner、ShellRunner、BrowserLauncher、PlatformAdapter
- Linux/macOS 兼容策略、能力声明和 TODO 清单写入架构文档
- 删除未完成的 Tauri、Rust、Sidecar 和桌面打包依赖
- Windows 本地服务冒烟、完整测试 43 passed、Ruff、Vite build 和 doctor 通过
- 新增用户、团队、团队/项目成员和会话共享契约及数据库表
- 新增团队管理员角色校验与 view/collaborate 会话共享授权
- Demo 固定 admin/admin 登录、进程内 Bearer token 和 Web 登录界面，不读取或修改系统环境变量
- 所有 HTTP 业务 API 统一验证 Bearer token，会话 WebSocket 验证 access_token
- 远程 Host 完成管理员登记、一次性配对码、Host token 和心跳链路；Host token 不授予工具执行权限

## 进行中

- 暂无记录

## 恢复后优先处理

- [ ] 进入 B7-B8 团队与发布阶段
- [ ] 后续独立实现 Linux/macOS 适配器并运行平台冒烟
- [ ] 在 B7 验收后执行性能、恢复、安全、部署和发布验收

## 阻塞与待确认

- 暂无记录

## 已确认约束

- 普通文档放在 docs/
- Python 依赖只由 uv 管理并提交 uv.lock
- 按计划书分批实施，每批可暂停、可验证、可交接
- 不默认执行高风险外部操作

## 工作区快照

- 顶层条目：.agents, .devpilot, .git, .github, .gitignore, .idea, .pnpm-store, .pytest_cache, .python-version, .ruff_cache, .uv-cache, AGENTS.md, alembic.ini, apps, docs, migrations, node_modules, packages, pnpm-lock.yaml, pnpm-workspace.yaml, pyproject.toml, tests, uv.lock, 项目重写计划书.md
- 交接文件默认位于 `docs/handovers/`。
- 进度事实来源为 `docs/progress.json`，人类可读视图为 `docs/PROGRESS.md`。

## 恢复检查清单

- [ ] 读取本文件与 `docs/PROGRESS.md`。
- [ ] 检查 `docs/progress.json` 是否需要更新。
- [ ] 执行 `uv sync --group dev` 同步环境。
- [ ] 执行 `uv run pytest` 和 `uv run ruff check .`。
- [ ] 完成当前批次后更新进度并重新生成交接文档。

## 生成方式

```powershell
uv run devpilot handover write --reason paused
```
