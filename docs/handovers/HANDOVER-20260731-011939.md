# DevPilot 交接文档

> 生成原因：real-agent-remediation-plan
> 生成时间：2026-07-31T01:19:39+08:00
> 总体完成度：90%

## 当前状态

- 当前批次：`B8`
- 下一步：按 docs/REAL_AGENT_IMPLEMENTATION_PLAN.md 从 R1 到 R7 补齐模型原生 Tool Calling、Agent 循环、项目级工具、上下文、持久化、验证和真实 DeepSeek E2E。
- 工作区：`C:\Users\Admin\Desktop\workspace\DevPilot`
- Git 状态：changes present

## 批次进度

| 批次 | 状态 | 完成度 | 说明 |
|---|---|---:|---|
| B0 阶段 0：基础骨架与交接机制 | 已完成 | 100% | Monorepo, FastAPI, CLI, 契约, 交接 Agent |
| B1 阶段 1：模型与 LangGraph 内核 | 已完成 | 100% | LangGraph, 模型网关, 运行生命周期 |
| B2 阶段 2：工具、策略与人工审批 | 已完成 | 100% | 工具, 策略, Shell/Git, 审批, 审计 |
| B3 阶段 3：数据、记忆与项目上下文 | 已完成 | 100% | 数据, 会话记忆, 项目规则, 长期记忆 |
| B4 阶段 4：研发工作流 | 已完成 | 100% | 仓库分析, 证据链, Agent 执行树, 测试, Worktree, PR |
| B5 阶段 5：CLI 与 Web 工作台 | 已完成 | 100% | CLI, React Web, 会话事件, 研发视图 |
| B6 阶段 6：Windows 本地 Web 与跨平台接口 | 已完成 | 100% | FastAPI, Vite, Windows 本地 Web, 平台端口 |
| B7 阶段 7：团队与共享 | 已完成 | 100% | 团队共享, RBAC, 远程 Host |
| B8 阶段 8：稳定化与发布 | 进行中 | 70% | 安全加固, 恢复, 迁移, 备份, 真实 Agent 闭环, Docker 发布 |

## 已完成

- 建立基础分层、FastAPI、进度事实源和交接 Agent
- 实现状态图、模型适配器、事件序列和 checkpoint
- 实现工具注册、策略引擎、Shell/Git 和审批审计
- 实现 SQLite/Alembic、会话、规则和长期记忆 API
- 实现仓库扫描、证据链、Agent 调度、测试和 PR 文档
- 实现 CLI、React/Vite 工作台、会话事件流和研发视图
- 完成 FastAPI/Vite 本地 Web、项目路径校验和平台端口
- 实现用户、团队、项目成员、会话共享、受保护 API/WebSocket 和远程 Host 配对
- 固定 B7 用户写入 users 表，并改用可跨重启验证的 HS256 JWT
- 补齐项目、工作流、PR 和记忆的资源级 RBAC 校验
- 持久化远程 Host 配对码摘要并使用带有效期的 Host JWT
- 添加 readiness、登录限流、附件限制、SQLite WAL 和安全响应头
- 添加 SQLite 备份命令、B8 Alembic 迁移、Dockerfile、Compose 和发布指南
- 通过完整 pytest、Ruff、doctor、Vite build 与迁移回归
- 完成多组模型连接、Coding Plan、环境变量回退、Web 配置和受限自动选模扩展
- 通过 83 项测试、设置/凭据模块 90% 覆盖率、Ruff、Vite build 和 DeepSeek 双协议最小真实调用

## 进行中

- 修复真实 Agent 闭环：当前模型只能返回代码文本，不能自主产生 Tool Call、写文件或运行测试
- 在 Docker 可用的环境执行 Compose 配置和容器启动健康检查

## 恢复后优先处理

- [ ] 后续独立实现 Linux/macOS 适配器并运行平台冒烟
- [ ] 执行 docs/REAL_AGENT_IMPLEMENTATION_PLAN.md 的 R1～R7
- [ ] 使用 DeepSeek 通过真实 FastAPI/WebSocket 在空目录自主创建 Event Lens 项目并运行测试
- [ ] 运行 docker compose config
- [ ] 运行 docker compose up --build -d 并验证 /healthz 与 /readyz

## 阻塞与待确认

- 真实 Agent E2E 已失败：Run 被标记 completed，但 Tool 事件、文件变化和测试执行均为 0
- 当前工作站未安装 Docker、docker-compose 或 Podman，无法执行容器发布验收

## 已确认约束

- 普通文档放在 docs/
- Python 依赖只由 uv 管理并提交 uv.lock
- 按计划书分批实施；每批可暂停、可验证、可交接
- 高风险外部操作必须经策略、审批和审计
- B7 固定用户 admin、admin1、admin2、admin3 在最终公开上线前保留，并通过 users、RBAC 和 JWT 正常链路授权

## 工作区快照

- 顶层条目：.agents, .codeassist, .coverage, .devpilot, .dockerignore, .git, .github, .gitignore, .idea, .pnpm-store, .pytest_cache, .python-version, .ruff_cache, .uv-cache, AGENTS.md, Dockerfile, README.md, alembic.ini, apps, deploy, docker, docker-compose.yml, docs, migrations, node_modules, packages, pnpm-lock.yaml, pnpm-workspace.yaml, pyproject.toml, start-devpilot.bat, tests, uv.lock, 项目重写计划书.md
- 交接文件默认位于 `docs/handovers/`。
- 进度事实来源为 `docs/progress.json`，人类可读视图为 `docs/PROGRESS.md`。

## 恢复检查清单

- [ ] 读取本文件与 `docs/PROGRESS.md`。
- [ ] 检查 `docs/progress.json` 是否需要更新。
- [ ] 执行 `uv sync --group dev` 同步环境。
- [ ] 执行 `uv run pytest` 和 `uv run ruff check .`。
- [ ] 完成当前批次后更新进度并重新生成交接文档。

## 生成方式

```powershell
uv run devpilot handover write --reason paused
```
